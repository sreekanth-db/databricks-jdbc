name: Integration Tests Workflow

on:
  push:
    branches: [main, benchmarking-branch]
  pull_request:
    types: [opened, synchronize, reopened]
    branches: [main, benchmarking-branch]
  workflow_dispatch:

jobs:
  build-and-test:
    name: Build and Run Integration Tests
    runs-on: ubuntu-latest
    environment: azure-prod
    strategy:
      matrix:
        include:
          - test-command: mvn -B compile test -Dtest=*IntegrationTests
            token-secret: DATABRICKS_TOKEN
          - test-command: mvn -B compile test -Dtest=**/fakeservice/tests/*.java,!**/fakeservice/tests/ConcurrencyIntegrationTests.java,!**/fakeservice/tests/PreparedStatementIntegrationTests.java
            token-secret: THRIFT_DATABRICKS_TOKEN
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v2
        with:
          java-version: 21
          distribution: 'adopt'
      - uses: actions/cache@v2
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2
      - name: Run Integration Tests
        run: ${{ matrix.test-command }}
        env:
          DATABRICKS_TOKEN: ${{ secrets[matrix.token-secret] }}
          DATABRICKS_USER: ${{ secrets.DATABRICKS_USER }}
